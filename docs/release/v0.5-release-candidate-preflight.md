# AgentPal v0.5 Release Candidate Preflight

This document records a local release-candidate preflight for AgentPal v0.5.

It is not a release announcement, not a tag, not a GitHub Release, and not a remote publication record.

## Local Status

Baseline status at the start of R194:

```text
master...origin/master [ahead 12]
working tree clean
```

R194 is expected to add local preflight evidence. If committed, the branch ahead count increases locally and still requires a separate user-authorized remote publication task.

## Scope

Checked areas:

- public README and Chinese README
- docs entry points
- PalSmith v0.5 user flow, capability summary, release prep, known limits, and release checklist
- official PalSmith Pal Pack identity, Skill entry, README, and `pal.json`
- `agentpal.json`
- `RESOURCE_INDEX.md`
- official Pal directory count
- central contacts boundary
- user custom Pal boundary for `FirstPrinciplesProductReviewer`
- Pal Asset Execution Contract, adoption guide, and fixture evidence
- no-code, runtime, Marketplace, contacts, official Pal, and remote publication claims

## Passed Checks

- AgentPal is described as a no-code Pal organization layer for existing Agent Runtimes.
- AgentPal is not described as an app runtime, desktop app, backend service, CLI runtime, daemon, scanner, connector, or Marketplace backend.
- Pal / Skill / Agent distinction is clear in public docs.
- PalSmith v0.5 path is clear from Pal idea to draft Pal Pack, user custom Pal, scoped authorization, revocation, and review.
- Draft Pal Packs and user custom Pals remain outside `official/pals/`.
- User custom Pals remain private by default and outside central contacts.
- Pal Asset Execution docs are present and linked.
- R199 real host rehearsal and R200 controlled write rehearsal are recorded as
  no-code evidence.
- R200 controlled write fixture is marked as a test artifact, not a real user
  custom Pal upgrade or official Pal.
- Discovery, invocation, delegation, contacts registration, and Marketplace draft work remain separate.
- Read-only and dry-run evidence remains labeled as such.
- `agentpal.json`, PalSmith `pal.json`, and the user custom Pal `pal.json` parse successfully.
- Official Pal directory count remains 10.
- No `workspace/organization/contacts/` diff was introduced.

## Minimal Fixes

R194 updates the official PalSmith README current-status section so it no longer foregrounds historical v0.1 / v0.2 publication status as the current state.

## Non-Blocking Notes

- Claude Code and generic CLI paths remain evidence-limited compared with the Codex-first path.
- User custom Pal discovery and invocation may require explicit host context unless the current runtime has current evidence for unified discovery.
- Remote publication remains a separate user-authorized task.
- R198-R201 do not prove every official Pal has been individually migrated to
  the Pal Asset Execution Contract.

## No-Code Boundary

R194 does not add:

- runtime code
- CLI
- installer
- scanner
- daemon
- connector
- backend service
- Marketplace backend
- official Pal
- central contacts mutation
- default public user custom Pal discovery
- all-official-Pal asset rewrite claims
- real Luma Pal upgrade claims

## Readiness Verdict

```text
ready_for_user_authorized_release
```

This verdict means the local workspace is ready for a user decision about GitHub sync, tag, and pre-release publication. It does not authorize or perform those remote actions.

R201 note: Pal Asset Execution adoption updates improve local release
readiness, but they do not change the remote authorization model in
`v0.5-user-authorization-decision.md`.
