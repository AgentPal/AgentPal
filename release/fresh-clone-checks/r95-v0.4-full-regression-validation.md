# R95 v0.4 Full Regression Validation

Date: 2026-06-28

Scope: public-safe local validation summary for the R95 v0.4 real-usage regression execution.

## Summary

All 10 R93-B v0.4 real-usage regression groups were executed locally.

| Check | Result | Evidence |
| --- | --- | --- |
| All 10 groups executed | pass | Results file lists Groups 1-10 as executed. |
| Required root paths missing | 0 | `standards`, `official`, `workspace`, `templates`, `examples`, `evals`, `release`, `archive`, and `docs` exist. |
| Working-tree JSON failures | 0 | 82 JSON files parsed successfully. |
| Clean-copy JSON failures | 0 | 55 JSON files parsed successfully in sanitized clean copy. |
| Thin binding result | pass | Generic and Claude temp projects contained only allowed binding files; project JSON parsed; forbidden dirs absent. |
| Central roster result | pass | `workspace/organization/contacts/pals.json` parsed; registered active Pal count: 9; `keyword_routing_allowed=false`. |
| Registry result | pass | `workspace/resources/registry/pal.index.json` item count: 9. |
| Official Pal result | pass | `official/pals/` count: 9; Level 0 assets present for every official Pal. |
| No keyword routing result | pass | No active JSON route keys; text hits are boundary/example/eval/regression or explicit mention docs. |
| Capability Inventory result | pass | Required path families exist; templates and examples parse; unknown / not-run / missing evidence docs exist. |
| Business System governance chain result | pass | Chain exists through Change Review Note with matching standards, templates, examples, and evals. |
| No scanner / connector / credential result | pass | Hits are negative policy text, safe examples, or forbidden failure examples; real leak count: 0. |
| Internal path leak result | pass | Internal development-directory public repo hits: 0 after R95 low-risk text fix. |
| Issue summary | pass | blocker 0, high 0, medium 0, low 2 fixed. |
| Forbidden Git / release actions | pass | No `git push`, `git pull`, `git fetch`, tag, or GitHub Release action executed in R95. |

## Thin Binding Temp Simulation

Sanitized temp base:

```text
%TEMP%/agentpal-r95-binding-20260628133328
```

Generic Codex project files:

```text
.agentpal/AGENTPAL.md
.agentpal/project.json
AGENTS.md
```

Claude Code project files:

```text
.agentpal/AGENTPAL.md
.agentpal/project.json
.claude/settings.local.json
.gitignore
CLAUDE.md
```

Forbidden default dirs absent:

```text
.agentpal/memory
.agentpal/state
.agentpal/reports
.agentpal/context
.agentpal/index
.agentpal/pals
.agentpal/workflows
.agentpal/evals
.agentpal/capability-inventory
.agentpal/business-systems
.agentpal/reviews
.agentpal/evidence
.agentpal/replay
.agentpal/rollback
.agentpal/verification
.agentpal/audit-trail
.agentpal/governance-decisions
.agentpal/change-ledger
.agentpal/change-review
```

## Clean Copy

Sanitized clean-copy path:

```text
%TEMP%/agentpal-r95-clean-final-20260628134142/AgentPal-clean-copy
```

The clean-copy run excluded `.git` and `exports`, then removed root-local `.agentpal`, `.codex`, and `.agents` while preserving public template `.agentpal` examples. Required public root directories existed and JSON parse failures were 0.

## Boundary Notes

R95 added only Markdown regression result, issue, validation, and low-risk text fixes. It did not add runtime code, scripts, package manifests, CLI, Web App, Desktop App, scanner, validator, installer, daemon, database, auto sync, auto execution engine, connector, API client, keyword router, deterministic semantic router, tag, GitHub Release, or external project thick `.agentpal/` content.
