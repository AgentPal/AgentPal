# R129 - v0.5 Regression Medium Issue Fix Plan

## Scope

R129 fixes only the two medium issues found by R128.

This is not a feature round, not a project-binding redesign, not an Org Pack / FDE expansion round, and not a GitHub release round.

## R128 Medium Issue 1

Issue: public files contained concrete private development-record path references.

Affected surface:

- historical Pal Asset evals;
- historical fresh-clone validation records;
- historical integration notes;
- one Pal template README and one Vega example that mentioned private reporting;
- R128 validation evidence.

Fix strategy:

- replace concrete private path strings with generic public-safe wording;
- keep the evidence meaning that a private completion report exists outside the public repository;
- do not copy private reports into the public repository;
- do not delete private reports outside the repository.

## R128 Medium Issue 2

Issue: current project-binding guidance still referenced legacy project-workgroup template paths.

Affected surface:

- `official/pals/Mira-main/core/project-workgroup-protocol.md`
- `examples/usage/join-external-project-workgroup.md`
- `examples/usage/external-project-mira-default.md`
- `orchestration/agent-instruction-file-loading-policy.md`
- `core/project-binding-thin-contract.md`
- `release/integration-notes/r127-v0.5-overall-regression-evidence-map.md`
- failure examples and runtime-adapter self-test wording that still used legacy copied-roster filenames

Fix strategy:

- point current guidance to `templates/project-binding/generic-codex/` and `templates/project-binding/claude-code/`;
- keep `.agentpal/project.json` and `.agentpal/AGENTPAL.md` as the current project-local binding files;
- keep root `AGENTS.md` and, for Claude Code, root `CLAUDE.md`, `.claude/settings.local.json`, and `.gitignore` as the current root/local surfaces;
- replace current references to legacy copied-roster and old install-prompt files with generic stale-file wording;
- keep archive and R69 compatibility records as historical context only.

## Forbidden Changes

- no central roster edits;
- no official Pal `pal.json` edits;
- no new official Pal manifest;
- no new runtime code, scanner, validator, connector, marketplace, installer, daemon, database, or API client;
- no external project writes;
- no push, pull, fetch, tag, or GitHub Release;
- no v0.5 completion claim.

## Validation Plan

R129 must pass:

- internal path leak scan: 0;
- current project-binding stale path scan: 0, excluding archive and explicit R69 historical compatibility records;
- all visible JSON parse;
- clean-copy validation;
- central roster 9/9;
- official Pal directories 9;
- official manifest count 1 and PalSmith only;
- no active keyword routing;
- no connector/scanner/marketplace active flags;
- refined secret scan with only the documented fake-token negative example;
- protected diff for central roster and official Pal metadata.
