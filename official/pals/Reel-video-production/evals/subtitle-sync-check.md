# Subtitle Sync Check

Pass requires:

- captions match spoken or intended timing;
- captions stay on screen long enough to read;
- emphasis highlights do not arrive before the word or beat;
- transcript checks are `pass`, `not-run`, or `blocked`.
