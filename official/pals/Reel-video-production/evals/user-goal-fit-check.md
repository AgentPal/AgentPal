# User Goal Fit Check

Pass requires:

- video structure supports the user's stated goal;
- audience and platform are clear;
- CTA is present when needed;
- final state is clear: `pass`, `requires-human-review`, `not-run`, `blocked`, or `fail`.
