# Memory

This folder contains release-safe memory structure only.

Runtime and private memory are ignored by Git. Do not store real user data, private project data, secrets, tokens, or credentials in release files.

