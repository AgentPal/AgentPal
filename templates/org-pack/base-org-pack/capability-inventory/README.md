# Capability Inventory References

Use this directory for public-safe capability profile references and notes.

Capability references may help AI judgement, but they must not become scanner output, automatic discovery output, connector configuration, or keyword routing.

Keep `unknown`, `not-run`, and `missing` evidence states explicit until real evidence exists.
