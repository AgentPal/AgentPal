# Pal Pack Templates

This lowercase directory is a navigation pointer for users who expect `templates/pal-pack/`.

The current template assets remain in:

- `templates/Pal Pack/zh-CN/`
- `templates/Pal Pack/en-US/`

Keep this directory as a compatibility pointer until the Pal Pack template paths are migrated in a dedicated compatibility-safe pass.
