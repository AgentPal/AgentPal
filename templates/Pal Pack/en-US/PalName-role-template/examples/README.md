# Examples

Store non-binding examples here.

Examples may show one possible usage pattern, but they must not become fixed routing rules.

If an example mentions another Pal, state that it is non-binding and that collaborators are still resolved case by case from the current `contacts/`, `registry/`, and AI judgement.
