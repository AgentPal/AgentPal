# Prompt Templates

This directory is the visible template home for prompt-related templates.

Copyable runtime and maintenance prompts currently live under the root `prompts/` directory. Keep root prompts there for compatibility; use this directory for future prompt-template families that are not runtime entry prompts.
