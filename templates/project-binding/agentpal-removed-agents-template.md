# AgentPal Workgroup Removed

This project is not currently connected to AgentPal.

Do not enter AgentPal Pal Mode for this project.
Do not treat ordinary messages as addressed to Mira.
Do not prefix ordinary answers with `Mira：`, `Atlas：`, `Rhea：`, or any other Pal name.
Answer as normal Codex unless the user explicitly asks to reconnect AgentPal or explicitly calls a Pal process from another active AgentPal workspace.

If a previous Codex thread already loaded AgentPal project-bound rules, those old conversation instructions may remain in that thread history even after files are removed. In that case, tell the user that the current old thread should be closed or restarted, or answer with `Codex generic answer:` when the user asks to continue without AgentPal.

AgentPal can be reconnected later by adding the AgentPal workgroup again.

