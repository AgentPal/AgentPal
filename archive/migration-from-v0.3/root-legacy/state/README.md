# Legacy State

This archived README records the old root `state/` placeholder. Runtime state is ignored by Git and should not be public release content.

State files should describe the current session only and must not contain secrets or private project data.
