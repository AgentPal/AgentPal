---
description: Initialize AgentPal thin binding for the current Claude Code project. Manual alias for /agentpal:enable.
when_to_use: Use when the user explicitly runs /agentpal:init.
argument-hint: [agentpal-path-or-github-url(optional)]
disable-model-invocation: true
---

# AgentPal Init

`/agentpal:init` is an alias of `/agentpal:enable`.

Follow the exact same behavior and boundaries as [../enable/SKILL.md](../enable/SKILL.md).
