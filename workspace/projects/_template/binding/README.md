# Binding

This directory records how an external user project is thin-bound to AgentPal.

The binding record is central. The external project should contain only thin entrypoint files such as `.agentpal/project.json`, `.agentpal/AGENTPAL.md`, and protected root instruction blocks.

