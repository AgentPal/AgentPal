# External Root

Record the exact external project root here.

- `active_project_root`: `REPLACE_WITH_EXTERNAL_PROJECT_ROOT`
- Evidence source:
- Last verified:

The external root is the user project. It is not the AgentPal Workspace.

